---
all:
  children:

    docker_hosts:

      hosts:

        debian-dev:

          ansible_host: 192.168.1.34

    k3s_hosts:

      hosts:

        k3s-server:

          ansible_host: 192.168.1.58
